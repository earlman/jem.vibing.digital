<!DOCTYPE html>
<html lang="en">

   <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <link href="{{ '/assets/fonts/anonymous-pro/index.css'}}" rel="stylesheet">
      <link rel="stylesheet" href="/styles/index.css">
      <title>🔥 {{title}}</title>
   </head>

   <body>
      {% block content %}{% endblock %}

      <footer>
         <div>
            {% if developerSettings.lyket %}

               <div
            class="lyket"
            data-lyket-type="like"
            data-lyket-id="{{baseUrl}}"
            data-lyket-namespace="lite"
            data-lyket-template="twitter"
            data-lyket-font-family="sans-serif"
            data-lyket-color-text="rgba(255,255,255,.5)"
      >
               {% endif %}
            </div>
         </div>
         <div class="logo">
            <a href="{{developerSettings.referralLink}}" target="_blank">
               {% include "logo-var1.svg" %}
            </a>
         </div>
      </footer>

      <style>

         :root {
            --starter-max-height: 1280px;
            --starter-max-width: 450px;
            --page-margin: 2rem;
         }

         @media (prefers-color-scheme: dark) {
            html {
               background-color: black;
            }
         }

         @media (prefers-color-scheme: light) {
            html {
               background-color: #ECEAEA;
            }
         }

         html {
            height: 100lvh;
            width: 100lvw;
            display: grid;
            place-content: center;
            position: relative;
         }

         body {
            height: 100lvh;
            width: 100lvw;
         }

         /* the value of media query below should be the same as --starter-max-width */
         @media (min-width: 720px) {
            body {
               max-width: var(--starter-max-width);
               max-height: calc(100lvh - calc(2* var(--page-margin)));
               border-radius: 10px;
               margin: auto;
            }
         }

         /* */

         footer {
            position: absolute;
            bottom: 0;
            place-self: flex-end center;
            display: grid;
            grid-template-columns: 1fr 1fr 1fr;
            width: 100%;
            justify-items: center;
            align-items: center;
         }

         footer > *:first-child {
            margin-left: var(--space-xs);
            place-self: flex-start;
         }

         footer .logo {
            margin-bottom: var(--space-2xs);
            max-width: 24px;
         }
      </style>

      {% if developerSettings.lyket %}
         <script src="https://unpkg.com/@lyket/widget@latest/dist/lyket.js?apiKey=pt_b2e989b79fbbbe5f5c7b7d42869d3d"></script>
      {% endif %}

   </html>